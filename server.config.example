# Este é um arquivo de exemplo para server.config
# Use-o para configurar seu servidor Minecraft.
# Copie este arquivo para 'server.config' e edite-o conforme necessário.

# A versão do Minecraft que você deseja rodar (ex: 1.20.1, 1.19.4)
VERSION="1.20.1"

# O tipo de servidor Minecraft (escolha um dos seguintes):
#   - VANILLA: Servidor oficial da Mojang.
#   - PAPER: Servidor otimizado com melhor performance e plugins (recomendado para a maioria).
#   - FORGE: Servidor para mods antigos (até 1.16.5).
#   - NEOFORGE: Servidor para mods mais recentes (a partir de 1.20.1).
#   - FABRIC: Servidor leve para mods (requer Fabric API para muitos mods).
#   - CUSTOM: Para modpacks que usam scripts de inicialização personalizados (startserver.sh, install.sh).
TYPE="PAPER"

# (Opcional) Versão específica do Mod Loader, se TYPE for FORGE, NEOFORGE ou FABRIC.
# Se deixado vazio, o script download-server.sh tentará buscar a versão mais recente compatível.
# Exemplos:
# FORGE: MOD_LOADER_VERSION="47.1.3"
# NEOFORGE: MOD_LOADER_VERSION="20.4.89-beta"
# FABRIC: MOD_LOADER_VERSION="0.15.11"
MOD_LOADER_VERSION=""

# Memória RAM mínima alocada para o servidor (ex: 1G, 2G, 4G, 8G).
# Use 'G' para Gigabytes e 'M' para Megabytes.
MIN_RAM="2G"

# Memória RAM máxima alocada para o servidor (ex: 2G, 4G, 8G, 12G).
# Deve ser igual ou maior que MIN_RAM.
MAX_RAM="4G"

# Argumentos adicionais para o servidor (após o -jar).
# Para servidores gráficos (GUI), remova "nogui". Para a maioria dos servidores dedicados, "nogui" é bom.
SERVER_ARGS="nogui"

# Argumentos adicionais para a JVM (Máquina Virtual Java).
# As otimizações G1GC são recomendadas para Minecraft.
# Para Paper/Fabric, é comum adicionar "-Dfile.encoding=UTF-8".
JAVA_ARGS="-XX:+UseG1GC -Dfile.encoding=UTF-8"

# (Opcional) Caminho completo para o arquivo JAR do servidor.
# O download-server.sh preencherá isso automaticamente após o download.
# Só defina manualmente se você já possui o JAR e quer usar um caminho específico.
# JAR_PATH="/caminho/para/seu/servidor.jar"
